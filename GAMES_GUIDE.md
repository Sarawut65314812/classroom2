# 🎮 คู่มือเกมใหม่ - เกมโยงเส้นจับคู่และเกมลูกปัดลอย

## 🎯 เกมโยงเส้นจับคู่ (Matching Line Game)

### ภาพรวม
เกมสำหรับฝึกทักษะการจับคู่และจดจำ โดยเด็กจะต้องลากเส้นเชื่อมรูปภาพหรือข้อความที่เข้าคู่กัน

### ฟีเจอร์หลัก
- ✅ ลากเส้นแบบ freehand (วาดเส้นอิสระ)
- ✅ รองรับ touch screen, stylus pen
- ✅ ตรวจสอบคำตอบทันที
- ✅ เส้นถูก = สีเขียว + ล็อก
- ✅ เส้นผิด = สีแดง + หายไป + เสียงผิด
- ✅ 3 ระดับความยาก (3, 5, 7 คู่)
- ✅ แสดงคะแนนแบบ real-time

### การใช้งาน

#### สำหรับนักเรียน:
1. เลือกระดับความยาก
2. กดเริ่มเล่น
3. ลากเส้นจากรูปซ้ายไปรูปขวาที่คิดว่าเข้าคู่กัน
4. ถ้าถูก: เส้นจะเป็นสีเขียวและล็อก
5. ถ้าผิด: เส้นจะเป็นสีแดงแล้วหายไป
6. จับคู่ให้ครบทุกคู่เพื่อชนะ

#### สำหรับครู (ในอนาคต):
- อัปโหลดรูปภาพซ้าย-ขวา
- กำหนดคู่ที่ถูกต้อง
- ตั้งชื่อกิจกรรม
- กำหนดคะแนน
- Preview ก่อนให้นักเรียนเล่น

### ตัวอย่างการใช้งาน

```typescript
// ตั้งค่าคู่สำหรับเกม
const pairs = [
  {
    id: 1,
    leftImage: 'path/to/apple.png',
    rightImage: 'path/to/text-apple.png',
    leftText: '🍎',
    rightText: 'แอปเปิ้ล'
  },
  // ... เพิ่มคู่อื่น ๆ
]

// ใช้งาน Component
<MatchingBoard
  pairs={pairs}
  onComplete={(score) => console.log('Score:', score)}
/>
```

### Props API

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| `pairs` | `MatchPair[]` | ✅ | รายการคู่ที่ต้องจับคู่ |
| `onComplete` | `(score: number) => void` | ❌ | Callback เมื่อจับคู่ครบทุกคู่ |

### MatchPair Interface

```typescript
interface MatchPair {
  id: number
  leftImage: string
  rightImage: string
  leftText?: string
  rightText?: string
}
```

---

## 🎮 เกมลูกปัดลอย (Floating Beads Game)

### ภาพรวม
เกมที่มีลูกปัดลอยไปมาบนหน้าจอ เด็กต้องโต้ตอบกับลูกปัดตามโหมดที่เลือก

### 2 โหมดเกม

#### 1️⃣ โหมดแตะตามลำดับ (Tap Sequence Mode)
- ลูกปัดมีหมายเลขกำกับ
- แตะตามลำดับ 1, 2, 3, 4...
- แตะถูก: ลูกปัดจางลง + เสียงถูก
- แตะผิด: ลูกปัดสั่น + เสียงผิด

#### 2️⃣ โหมดลากใส่ตะกร้า (Drag Into Basket Mode)
- ลูกปัดหลายสี (แดง, น้ำเงิน, เขียว, เหลือง)
- ตะกร้า 4 ใบตามสี
- ลากลูกปัดสีแดงใส่ตะกร้าแดง (และสีอื่น ๆ)
- ถูก: ลูกปัดหายไป + เสียงถูก + คะแนน
- ผิด: ลูกปัดกลับมาที่เดิม

### ฟีเจอร์หลัก
- ✅ ลูกปัดลอยด้วย animation (requestAnimationFrame)
- ✅ Bounce off walls (ตีกลับเมื่อชนขอบ)
- ✅ รองรับ drag & drop แบบ touch
- ✅ ปรับจำนวนลูกปัดได้ (3-10 ลูก)
- ✅ เสียงตอบสนอง (ถูก/ผิด)
- ✅ นับคะแนนแบบ real-time

### การใช้งาน

#### สำหรับนักเรียน:
1. เลือกโหมด (แตะตามลำดับ หรือ ลากใส่ตะกร้า)
2. ปรับจำนวนลูกปัด (3-10)
3. กดเริ่มเล่น
4. **โหมดแตะ**: แตะลูกปัดตามหมายเลข
5. **โหมดตะกร้า**: ลากลูกปัดใส่ตะกร้าสีเดียวกัน

#### สำหรับครู (ในอนาคต):
- เลือกโหมด
- กำหนดจำนวนและสีลูกปัด
- ตั้งลำดับแบบ custom (โหมดแตะ)
- กำหนดตะกร้าและกฎ (โหมดตะกร้า)
- ตั้งความเร็วลูกปัด
- ตั้งคะแนน

### ตัวอย่างการใช้งาน

```typescript
// โหมดแตะตามลำดับ
<FloatingBeadsBoard
  mode="sequence"
  beadCount={6}
  sequence={[0, 1, 2, 3, 4, 5]}
  onComplete={(score) => console.log('Score:', score)}
/>

// โหมดลากใส่ตะกร้า
const baskets = [
  {
    id: 'red',
    x: 100,
    y: 450,
    width: 150,
    height: 100,
    color: '#ef4444',
    label: '🔴 แดง',
    acceptedColors: ['#ef4444']
  },
  // ... เพิ่มตะกร้าอื่น ๆ
]

<FloatingBeadsBoard
  mode="basket"
  beadCount={6}
  baskets={baskets}
  onComplete={(score) => console.log('Score:', score)}
/>
```

### Props API

| Prop | Type | Required | Description |
|------|------|----------|-------------|
| `mode` | `'sequence' \| 'basket'` | ✅ | โหมดเกม |
| `beadCount` | `number` | ❌ | จำนวนลูกปัด (default: 6) |
| `sequence` | `number[]` | ❌ | ลำดับสำหรับโหมด sequence |
| `baskets` | `Basket[]` | ❌ | ตะกร้าสำหรับโหมด basket |
| `onComplete` | `(score: number) => void` | ❌ | Callback เมื่อเล่นจบ |

### Interfaces

```typescript
interface Bead {
  id: number
  x: number
  y: number
  vx: number  // ความเร็วแกน X
  vy: number  // ความเร็วแกน Y
  color: string
  radius: number
  sequenceOrder?: number
  basketId?: string
  isActive: boolean
  isTapped?: boolean
}

interface Basket {
  id: string
  x: number
  y: number
  width: number
  height: number
  color: string
  label: string
  acceptedColors: string[]
}
```

---

## 🎨 การปรับแต่ง UI

### สีของเกม

แก้ไขใน CSS files:

**MatchingBoard.css**
```css
/* เปลี่ยนสีพื้นหลัง */
.play-matching-page {
  background: linear-gradient(135deg, #ffecd2 0%, #fcb69f 100%);
}

/* เปลี่ยนสีเส้นถูก */
.correct-line {
  stroke: #22c55e; /* เขียว */
}

/* เปลี่ยนสีเส้นผิด */
.wrong-line {
  stroke: #ef4444; /* แดง */
}
```

**FloatingBeadsBoard.css**
```css
/* เปลี่ยนสีพื้นหลัง canvas */
.canvas-container {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}
```

### ปรับขนาดลูกปัดและตะกร้า

แก้ไขใน `FloatingBeadsBoard.tsx`:

```typescript
const BEAD_RADIUS = 30  // เปลี่ยนขนาดลูกปัด (default: 30)

// ปรับขนาดตะกร้า
const basket = {
  width: 150,   // ความกว้าง
  height: 100,  // ความสูง
  // ...
}
```

### เปลี่ยนสีลูกปัด

```typescript
const BEAD_COLORS = [
  '#ef4444',  // แดง
  '#3b82f6',  // น้ำเงิน
  '#22c55e',  // เขียว
  '#f59e0b',  // เหลือง
  '#a855f7',  // ม่วง
  '#ec4899'   // ชมพู
]
```

---

## 🔧 Technical Details

### Performance Optimization

**MatchingBoard:**
- ใช้ `useMemo` สำหรับการคำนวณตำแหน่ง
- Lazy load รูปภาพ
- Event delegation สำหรับ touch events

**FloatingBeadsBoard:**
- ใช้ `requestAnimationFrame` สำหรับ smooth animation
- Cancel animation frame เมื่อ component unmount
- Optimize collision detection

### Browser Compatibility
- ✅ Chrome 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ✅ Edge 90+
- ✅ Mobile browsers (iOS Safari, Chrome Mobile)

### Touch Support
- Single touch
- Multi-touch (ใน future update)
- Stylus pen
- Mouse (สำหรับ desktop)

---

## 📱 การใช้งานบน Tablet/Smartboard

### แนะนำการตั้งค่า

1. **ขนาดหน้าจอ**: 1024x768 ขึ้นไป
2. **Browser**: Chrome/Safari แบบ fullscreen
3. **Touch**: เปิด touch calibration
4. **Zoom**: ตั้งเป็น 100%

### Keyboard Shortcuts (สำหรับครู)

- `Esc` - กลับหน้าหลัก
- `R` - Restart เกม
- `H` - แสดง/ซ่อน hint (ในอนาคต)

---

## 🐛 Troubleshooting

### ลูกปัดไม่ลอย
- ตรวจสอบว่า `requestAnimationFrame` ทำงาน
- เช็ค browser console สำหรับ errors

### เส้นโยงไม่ปรากฏ
- ตรวจสอบว่ามีการ import `react-konva` ถูกต้อง
- เช็คว่า canvas render แล้ว

### Touch ไม่ work
- ตรวจสอบ touch events: `onTouchStart`, `onTouchMove`, `onTouchEnd`
- ลอง refresh browser
- เช็ค touch calibration ของอุปกรณ์

---

## 🚀 Future Enhancements

### Matching Game
- [ ] อัปโหลดรูปแบบ custom จากครู
- [ ] โหมดเล่นแบบจับเวลา
- [ ] Multiplayer mode
- [ ] เสียงเฉพาะแต่ละคู่

### Beads Game
- [ ] โหมด pattern matching (จับลูกปัดตามลวดลาย)
- [ ] โหมด color sorting with levels
- [ ] Physics-based interactions
- [ ] Power-ups และ obstacles

---

## 📞 Support

หากพบปัญหาหรือต้องการความช่วยเหลือ:
- อ่าน README.md หลัก
- ตรวจสอบ browser console
- ลองรีเฟรชหน้าเว็บ

**Happy Gaming! 🎮✨**
